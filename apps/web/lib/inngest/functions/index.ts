/**
 * Barrel export for all Inngest functions.
 *
 * Functions are added here as user story automation is implemented:
 * - US1/US7 (T098): appointment reminders, waitlist notifications
 * - US4 (T098): membership renewal warnings, welcome sequences
 * - US5 (T098): low-credit alerts
 *
 * The serve handler in apps/web/app/api/inngest/route.ts imports from here.
 */

// Placeholder export — remove once real functions are added
export const functions: never[] = [];
